{
    "scripts": {
        "watch": "nodemon --ext ts --watch src --exec node -r esbuild-register ",
        "check": "tsc --watch",
        "main": "node -r esbuild-register src/main.ts",
        "test": "vitest"
    },
    "devDependencies": {
        "@types/node": "^18.7.13",
        "esbuild": "^0.15.5",
        "esbuild-register": "^3.3.3",
        "nodemon": "^2.0.19",
        "prettier": "^2.7.1",
        "typescript": "^4.8.2",
        "vite-tsconfig-paths": "^3.5.0",
        "vitest": "^0.22.1"
    }
}
